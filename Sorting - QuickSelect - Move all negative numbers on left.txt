#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    cin>>arr[i];
    int l=-1,r=0;
    while(r<n){
        if(arr[r] < 0){
            l++;
            swap(arr[l],arr[r]);
        }
        r++;
    }
    for(int i=0;i<n;i++)
    cout<<arr[i]<<" ";
    return 0;
}
