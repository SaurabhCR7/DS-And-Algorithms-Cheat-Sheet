for(int j=a;j<=b;j++){
  temp=j;
  while(temp>0){
    if(temp&1)
    count++;
    temp = temp>>1;
  }

  while(temp){
    count++;
    temp = temp&(temp-1);
  }
			
  count = count+ __builtin_popcount(temp);
}